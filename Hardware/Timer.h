#ifndef __TIMER_H
#define __TIMER_H

// ==========================================
// 模块名称：Timer（定时器模块）
// 功能说明：配置TIM2中断，用于周期性执行编码器读取、PID计算、PWM更新和数据发送
// ==========================================

void Timer_Init(void);  // 初始化TIM2定时器及中断

#endif
